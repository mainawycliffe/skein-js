# This graph is deterministic and needs no secrets. The file exists to show that `skein dev` loads
# `langgraph.json`'s `env` (and a conventional `.env`) into `process.env` at boot, exactly like
# `langgraph dev`. Copy to `.env` and add anything a real graph would need.

# EXAMPLE_GREETING=hello
