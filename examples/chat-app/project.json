{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "example-chat-app",
  "sourceRoot": "examples/chat-app/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "examples/chat-app"
      }
    }
  }
}
