# Frontend config. Copy to .env.local; the defaults already match `skein dev` + this example's graph.

# skein-js server base URL (matches `skein dev --port 2024`).
NEXT_PUBLIC_SKEIN_URL=http://localhost:2024

# The graph id from langgraph.json.
NEXT_PUBLIC_SKEIN_ASSISTANT_ID=research
